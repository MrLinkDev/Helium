<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:editText="clr-namespace:Helium.Controls.EditText"
                    xmlns:system="clr-namespace:System;assembly=System.Runtime">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Helium;component/Resources/Brushes.xaml"/>
        <ResourceDictionary Source="/Helium;component/Resources/Colors.xaml"/>
        <ResourceDictionary Source="/Helium;component/Resources/Dimensions.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <Style TargetType="{x:Type editText:HeEditText}">
        <Setter Property="Height" Value="{StaticResource Height}"/>
        <Setter Property="Width" Value="{StaticResource Width}"/>
        
        <Setter Property="FontSize" Value="{StaticResource FontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}"/>
        
        <Setter Property="CaretBrush" Value="{StaticResource WhiteBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource PrimaryBrush500}"/>
        
        <Setter Property="Padding" Value="4, 0, 0, 0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type editText:HeEditText}">
                    <Border
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        
                        Background="{TemplateBinding Background}"
                        
                        BorderBrush="{TemplateBinding EditTextBorder}"
                        BorderThickness="{StaticResource Thickness2}"
                        
                        CornerRadius="{StaticResource Radius4}"
                        VerticalAlignment="Center">
                        
                        <Grid>
                            <Label
                                x:Name="PART_Hint"
                                
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                
                                Background="Transparent"
                                Foreground="{StaticResource BlackBrush500}"
                                
                                FontSize="{TemplateBinding FontSize}"
                                Visibility="Collapsed"
                                
                                VerticalAlignment="Center"
                                VerticalContentAlignment="Center"
                                
                                Content="{TemplateBinding EditTextHint}"/>
                            
                            <ScrollViewer 
                                x:Name="PART_ContentHost"
                            
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                            
                                VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Resources>
                        <VisualBrush
                            x:Key="EditTextHint"
                            
                            AlignmentX="Left"
                            AlignmentY="Center"
                            
                            Stretch="None">

                            <VisualBrush.Visual>
                                <Label
                                    Foreground="{StaticResource BlackBrush500}"
                                    Background="Transparent"
                                    
                                    VerticalContentAlignment="Center"
                                    HorizontalContentAlignment="Left"
                                    
                                    Content="{TemplateBinding EditTextHint}" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </ControlTemplate.Resources>
                    
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="{x:Static system:String.Empty}">
                            <Setter Property="Visibility" TargetName="PART_Hint" Value="Visible"/>
                        </Trigger>
                        
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter Property="Visibility" TargetName="PART_Hint" Value="Visible"/>
                        </Trigger>
                        
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Visibility" TargetName="PART_Hint" Value="Collapsed"/>
                        </Trigger>
                        
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="EditTextBorder" Value="{StaticResource BlackBrush400}"/>
                        </Trigger>
                        
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="EditTextBorder" Value="{StaticResource PrimaryBrush500}"/>
                        </Trigger>
                        
                        <Trigger Property="IsSelectionActive" Value="True">
                            <Setter Property="EditTextBorder" Value="{StaticResource PrimaryBrush500}"/>
                        </Trigger>
                        
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="EditTextBorder" Value="{StaticResource BlackBrush700}"/>
                            <Setter Property="Foreground" Value="{StaticResource BlackBrush700}"/>
                        </Trigger>
                        
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter Property="EditTextBorder" Value="{StaticResource AttentionBrush500}"/>
                            <Setter Property="SelectionBrush" Value="{StaticResource AttentionBrush500}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>