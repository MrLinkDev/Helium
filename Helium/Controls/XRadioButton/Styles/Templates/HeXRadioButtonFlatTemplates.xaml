<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Helium.Controls.XRadioButton">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Helium;component/Resources/Brushes.xaml" />
        <ResourceDictionary Source="/Helium;component/Resources/Colors.xaml" />
        <ResourceDictionary Source="/Helium;component/Resources/Dimensions.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="HeXRadioButtonFlatDefault" TargetType="{x:Type local:HeXRadioButton}">
        <Border
            Width="{TemplateBinding Width}"
            Height="{TemplateBinding Height}"

            Background="{TemplateBinding Background}"

            CornerRadius="{TemplateBinding CornerRadius}"

            SnapsToDevicePixels="True">
            
            <DockPanel
                LastChildFill="False">
                <Grid
                    DockPanel.Dock="Left"
                    SnapsToDevicePixels="True">
                    <Ellipse
                        Height="{StaticResource EllipseSize}"
                        Width="{StaticResource EllipseSize}"
                            
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                            
                        Fill="{TemplateBinding EllipseFill}"
                            
                        Stroke="{TemplateBinding EllipseStroke}"
                        StrokeThickness="{StaticResource EllipseStrokeSize}">
                    </Ellipse>
                        
                    <Ellipse
                        Height="{StaticResource EllipseInnerSize}"
                        Width="{StaticResource EllipseInnerSize}"
                            
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                            
                        Fill="{TemplateBinding EllipseInnerFill}"/>
                </Grid>
                        
                <ContentPresenter
                    DockPanel.Dock="Left"
                    Margin="4, 0, 0, 0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Content="{TemplateBinding Content}"/>
            </DockPanel>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="False">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush500}" />
            </Trigger>
            
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush600}" />
                <Setter Property="EllipseStroke" Value="{StaticResource PrimaryBrush600}"/>
                <Setter Property="EllipseInnerFill" Value="{StaticResource PrimaryBrush800}"/>
            </Trigger>
                        
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="EllipseInnerFill" Value="{StaticResource GreenBrush500}"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush700}" />
                <Setter Property="EllipseStroke" Value="{StaticResource PrimaryBrush700}"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush950}" />

                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush700}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>